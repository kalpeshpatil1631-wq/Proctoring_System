config:
  target: "http://localhost:5000"
  phases:
    - duration: 60
      arrivalRate: 5
      name: Warm up
    - duration: 120
      arrivalRate: 10
      rampTo: 50
      name: Ramp up load
  plugins:
    # ensure: {}
    # apdex: {}
    # metrics-by-endpoint: {}
  # ensure:
  #   p95: 100
  #   maxErrorRate: 1

scenarios:
  - flow:
      - get:
          url: "/"
      - get:
          url: "/static/js/firebase-config.js"
